"""Label Studio integration.

Import the concrete module (``from digitex.labeling.client import
LabelStudioClient``) rather than this package, so there is only ever one
spelling for a name — the same rule ``pipeline`` and ``ui`` follow.
"""
