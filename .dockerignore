# Secrets
.env
.env.*
*.pem

# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Git
.git/

# Test / lint
.pytest_cache/
.ruff_cache/
.agents/
.opencode/
*.coverage
htmlcov/

# Data
data/

# Large files not needed at runtime. var/ is the data root — the book archive,
# model weights and the extraction output tree; the bot is served its corpus
# through a bind mount, never from the image.
*.pt
var/
logs/
configs/
tools/
docs/
drafts/
